[package]
name = "gcal-search"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
chrono = "0.4"
rand = "0.8"
serde = { version = "1", features = ["derive"] }
toml = "0.8"
rust_xlsxwriter = "0.99.0"
